# Plan: IV Number Rowspan Implementation for capit-list.xsl

## Goal
Modify xslt/capit-list.xsl:79-123 to display IV numbers with rowspan when they appear across multiple BK items, creating a grouped table layout where each IV number appears once with its associated BK items listed in subsequent rows.

## Context
- Current data structure: BK items contain nested IV items via `list[@type='CNS']/item`
- Current iteration: loops over IV items, displaying each with its parent BK info
- Desired output: group by IV number, display IV once with rowspan, then list all associated BK items

## Implementation Strategy

### 1. Replace Current Iteration (lines 79-123)
Use XSLT 3.0's `xsl:for-each-group` to invert the BK[IV] relationship to IV[BK]:

```xml
<xsl:for-each-group select=".//tei:item[tei:list[@type='CNS']/tei:item]//tei:list[@type='CNS']/tei:item"
                    group-by="normalize-space(.)">
  <xsl:sort select="concat(format-number(xs:integer(if (matches(substring-after(normalize-space(@xml:id),'IV_'), '^[0-9]+')) then replace(substring-after(normalize-space(@xml:id),'IV_'),'[^0-9].*','') else '0'),'000'), replace(substring-after(normalize-space(@xml:id),'IV_'),'^[0-9]+',''))"
            data-type="text" order="ascending"/>

  <xsl:variable name="group-size" select="count(current-group())"/>
  <xsl:variable name="iv-text" select="current-grouping-key()"/>

  <!-- First row with IV cell and rowspan -->
  <tr>
    <td class="siglum" rowspan="{$group-size}">
      <xsl:value-of select="$iv-text"/>
    </td>
    <td class="title">
      <xsl:call-template name="if-visible">
        <xsl:with-param name="path" select="concat('/capit/', current-group()[1]/ancestor::tei:item[1]/tei:name/@ref, '/')"/>
        <xsl:with-param name="title">
          <xsl:text>[:de]Zu[:en]Go to[:] </xsl:text>
          <xsl:value-of select="$iv-text"/>
        </xsl:with-param>
        <xsl:with-param name="text" select="normalize-space(current-group()[1]/ancestor::tei:item[1]/tei:name)"/>
      </xsl:call-template>
    </td>
    <td>
      <xsl:value-of select="cap:human-readable-siglum(current-group()[1]/ancestor::tei:item[1]/@xml:id)"/>
    </td>
  </tr>

  <!-- Subsequent rows without IV cell -->
  <xsl:for-each select="current-group()[position() > 1]">
    <tr>
      <td class="title">
        <xsl:call-template name="if-visible">
          <xsl:with-param name="path" select="concat('/capit/', ancestor::tei:item[1]/tei:name/@ref, '/')"/>
          <xsl:with-param name="title">
            <xsl:text>[:de]Zu[:en]Go to[:] </xsl:text>
            <xsl:value-of select="$iv-text"/>
          </xsl:with-param>
          <xsl:with-param name="text" select="normalize-space(ancestor::tei:item[1]/tei:name)"/>
        </xsl:call-template>
      </td>
      <td>
        <xsl:value-of select="cap:human-readable-siglum(ancestor::tei:item[1]/@xml:id)"/>
      </td>
    </tr>
  </xsl:for-each>
</xsl:for-each-group>
```

### 2. Key Changes
- Replace lines 79-123 with the grouping logic above
- Maintain all existing sorting, linking, and formatting behavior
- The `rowspan` attribute on the first `<td>` in each group creates the merged cell effect

## Validation Steps
1. Test with IV numbers that appear in multiple BK items (e.g., IV_38 in BK_184 and BK_186)
2. Verify sorting order remains correct (numeric then alphabetic suffix)
3. Confirm all links still resolve properly
4. Check that single IV items (with only one BK) display without rowspan
5. Validate HTML output structure

## Risks
- XSLT 3.0 `xsl:for-each-group` compatibility (verify processor support)
- Path navigation from grouped IV items to parent BK items may need adjustment
- CSS styling may need updates to accommodate rowspan layout

## Affected Files
- `xslt/capit-list.xsl` (lines 79-123)

## Open Questions
None - implementation is ready to proceed.
